<%= form_with(model: metadata_profile, url: account_profiles_path(@account)) do |form| %>
<%= render "shared/error_messages", resource: form.object %>

  <div class="inline">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>