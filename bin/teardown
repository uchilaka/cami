#!/usr/bin/env zsh

# Get the project root directory
PROJECT_ROOT=$(git rev-parse --show-toplevel)

docker compose down &&\
  rm -vRf "$PROJECT_ROOT/db/development/postgres" &&\
    rm -vRf "$PROJECT_ROOT/db/development/redis"
